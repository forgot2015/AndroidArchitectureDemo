<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.linzongfu.myarchitecture">

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyArchitecture">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".mvc.MvcActivity"
            android:label="MvcActivity" />
        <activity
            android:name=".mvp.MvpActivity"
            android:label="MvpActivity" />
        <activity
            android:name=".mvvm.MvvmActivity"
            android:label="MvvmActivity" />

    </application>

</manifest>